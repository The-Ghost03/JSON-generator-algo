#include <stddef.h>
#include "parser_json.h"

/* stub minimal pour compiler */
Graph *parser_json_load(const char *filename)
{
    (void)filename; /* évite warning “unused parameter” */
    return NULL;    /* on implémentera plus tard */
}
